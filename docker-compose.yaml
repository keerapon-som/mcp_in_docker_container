version: '3.8'

services:
  mcp-server:
    image: youtubedownloadmcp:latest
    environment:
      - OUTPUT_DIR=yim_output
    volumes:
      - /Users/keerapon.som/Desktop/Gem/Personal Gem/Road To Legendary Opensource Developer/mcp_in_docker_container/local_output:/app/yim_output

networks:
  mcp-network:
    driver: bridge